[package]
name = "crawler-parameters"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
crawler-quantity = { path = "../crawler-quantity" }
serde = { version = "1", features = ["derive"] }
thiserror = "2"

[dev-dependencies]
serde_json = "1"

[lints.rust]
unsafe_code = "forbid"
