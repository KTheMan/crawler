[package]
name = "crawler-storage"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
crawler-document = { path = "../crawler-document" }
crawler-history = { path = "../crawler-history" }
crawler-package = { path = "../crawler-package" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
